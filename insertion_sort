
void insertion_sort(int arr[],int n)
{
	for (int i = 0; i < n; i++)
	{
		int j = i ;
		while (j >= 0 )
		{
			if (arr[j] < arr[j - 1])
			{
				swap(j, j - 1, arr);
				j--;
			}
			else
				break;
		}
		cout << endl;
	}
}
